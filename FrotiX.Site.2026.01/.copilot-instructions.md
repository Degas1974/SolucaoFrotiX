# GitHub Copilot – Instruções do Projeto FrotiX

> **Sistema:** GitHub Copilot (Chat & Inline Suggestions)
> **Versão:** 1.0
> **Última Atualização:** 18/01/2026

---

## 🎯 DIRETÓRIO PADRÃO DE TRABALHO

**ESTE É O DIRETÓRIO ATIVO PRINCIPAL:** `FrotiX.Site.2026.01/`

⚠️ **IMPORTANTE:**
- ✅ **SEMPRE** trabalhe neste diretório (`FrotiX.Site.2026.01`)
- ✅ Todos os caminhos relativos partem daqui
- ✅ Ao buscar arquivos, priorize esta pasta
- ❌ `FrotiX.Site`, `FrotiX.Site.Novo`, `FrotiX.Site.Q4`, `FrotiX.Site.Backup` são **LEGADOS**
- ❌ Não sugira modificações em projetos legados

---

## 🚨 PROTOCOLO DE INICIALIZAÇÃO

### AÇÕES OBRIGATÓRIAS

Antes de sugerir código ou responder no chat:

1. ✅ Ler completamente: `RegrasDesenvolvimentoFrotiX.md`
2. ✅ Se trabalhar com banco: Ler `FrotiX.sql`
3. ✅ Confirmar leitura internamente

---

## 📋 HIERARQUIA DE REGRAS

| Arquivo | Função | Prioridade |
|---------|--------|-----------|
| `RegrasDesenvolvimentoFrotiX.md` | ⭐ Fonte oficial de regras | **MÁXIMA** |
| `.copilot-instructions.md` | Instruções Copilot | Alta |
| `FrotiX.sql` | Estrutura do banco | Alta (quando DB) |

---

## 🧠 MEMÓRIA PERMANENTE

### Adicionando Regras ao Projeto

**Quando o usuário disser:**
- "memorize"
- "guarde isso"
- "adicione às regras"
- "sempre faça X"

**Execute:**

```bash
# 1. Abrir
vim RegrasDesenvolvimentoFrotiX.md

# 2. Adicionar na seção apropriada
### [N].[N] [Nome da Regra]

**Contexto:** [Por que existe]
**Regra:** [O que fazer/evitar]
**Exemplo:**
```[lang]
// código
```
**Data:** DD/MM/AAAA

# 3. Commitar
git add RegrasDesenvolvimentoFrotiX.md
git commit -m "docs: Adiciona regra - [Nome]"
git push

# 4. Confirmar ao usuário
"✅ Regra memorizada em RegrasDesenvolvimentoFrotiX.md"
```

---

## ⚠️ REGRAS CRÍTICAS

### Try-Catch (OBRIGATÓRIO)
```csharp
// ✅ SEMPRE
public IActionResult Action()
{
    try { /* código */ }
    catch (Exception e)
    {
        Alerta.TratamentoErroComLinha("File.cs", "Action", e);
        return Json(new { success = false, message = e.Message });
    }
}
```

### Alertas (SweetAlert OBRIGATÓRIO)
```javascript
// ✅ USAR
Alerta.Sucesso("Título", "Mensagem");
Alerta.Erro("Título", "Mensagem");
await Alerta.Confirmar("Título", "Mensagem", "Sim", "Não");

// ❌ NUNCA
alert("mensagem");
confirm("mensagem");
```

### Ícones (Duotone OBRIGATÓRIO)
```html
<!-- ✅ SEMPRE -->
<i class="fa-duotone fa-car" style="--fa-primary-color:#ff6b35; --fa-secondary-color:#6c757d;"></i>

<!-- ❌ NUNCA -->
<i class="fa-solid fa-car"></i>
```

### Loading (FtxSpin OBRIGATÓRIO)
```javascript
// ✅ USAR
FtxSpin.show("Processando...");
// ... operação
FtxSpin.hide();

// ❌ NUNCA
<div class="spinner-border"></div>
```

### Banco de Dados
```yaml
Antes de codificar:
  - Consultar: FrotiX.sql
  - Verificar: Nomes de colunas, tipos, nullable
  - Nunca: Assumir estrutura de memória
```

---

## 📝 DOCUMENTAÇÃO

Sempre atualizar `Documentacao/` antes de commitar:

```markdown
# Formato
- Prosa técnica explicativa
- Snippets de código
- Explicação linha-a-linha
- Log de modificações
```

---

## 🔄 GIT

```yaml
Branch: main (preferencial)
Push: SEMPRE para main (nunca outras branches)
Commit: Automático IMEDIATO após código importante
Push: Automático IMEDIATO após commit (origin main)
Durante conversa: Commit automático ao fornecer código importante
Tipos: feat, fix, refactor, docs, style, chore
Exceção: Só não commitar se usuário pedir explicitamente
```

---

## 📚 REFERÊNCIAS

| Arquivo | Descrição |
|---------|-----------|
| `RegrasDesenvolvimentoFrotiX.md` | ⭐ LEITURA OBRIGATÓRIA |
| `FrotiX.sql` | Estrutura do banco |
| `wwwroot/js/alerta.js` | Sistema de alertas |
| `wwwroot/js/frotix.js` | Utilitários JS |

---

## 🎯 INLINE SUGGESTIONS

### Quando sugerir código:

1. ✅ Sempre incluir try-catch
2. ✅ Usar Alerta.* (não alert)
3. ✅ Usar fa-duotone (não fa-solid)
4. ✅ Usar FtxSpin (não spinner bootstrap)
5. ✅ Verificar FrotiX.sql antes de sugerir queries

### Auto-Complete Intelligence:

```csharp
// Quando detectar função sem try-catch, sugerir:
public IActionResult MinhaAction()
{
    try
    {
        // [seu código aqui]
    }
    catch (Exception error)
    {
        Alerta.TratamentoErroComLinha("Controller.cs", "MinhaAction", error);
        return Json(new { success = false, message = error.Message });
    }
}
```

---

## 💡 CONFIGURAÇÃO VS CODE

Adicione ao `.vscode/settings.json`:

```json
{
  "github.copilot.advanced": {
    "instructionFiles": [
      ".copilot-instructions.md",
      "RegrasDesenvolvimentoFrotiX.md"
    ]
  }
}
```

---

**✅ Copilot configurado para FrotiX. Sugestões alinhadas com padrões do projeto.**
