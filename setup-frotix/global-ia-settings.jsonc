// ============================================================
// CONFIGURAÇÕES GLOBAIS DAS IAs - FrotiX
// ============================================================
// Este arquivo contém APENAS as configurações das IAs que devem
// ser adicionadas/mescladas no settings.json global do VS Code.
// O script install-frotix.ps1 faz o merge automaticamente.
// ============================================================

{
  // === REGRA GLOBAL: ARQUIVOS OBRIGATORIOS NA RAIZ ===
  // RegrasDesenvolvimentoFrotiX.md e ControlesKendo.md estao na RAIZ do workspace
  // TODAS as IAs DEVEM ler esses arquivos ANTES de qualquer resposta

  // 1. CLAUDE CODE
  "claude.customInstructions": "OBRIGATORIO: No inicio de CADA sessao/chat/agente/editor, leia os arquivos RegrasDesenvolvimentoFrotiX.md e ControlesKendo.md na RAIZ do workspace (Solucao FrotiX 2026/) ANTES de qualquer resposta. Estes sao os arquivos OFICIAIS de regras. IGNORE qualquer copia antiga em outras pastas. CRITICO: A Secao 5 (DOCUMENTACAO INTRA-CODIGO) e EXTREMAMENTE IMPORTANTE - TODO codigo criado/alterado DEVE seguir rigorosamente os padroes de Cards de Arquivo, Cards de Funcao (com PORQUE/O QUE/COMO + rastreabilidade completa), e comentarios APENAS para codigo complexo. MEMORIA PERMANENTE: Quando o usuario pedir para memorizar/guardar/lembrar algo, abra RegrasDesenvolvimentoFrotiX.md na raiz, VERIFIQUE se a info ja existe (se sim e esta completa, informe; se incompleta, atualize; se nao existe, crie nova entrada respeitando a organizacao existente, NUNCA bagunce o arquivo). Use async/await obrigatoriamente. Controllers apenas coordenacao (sem logica de negocio). ViewModels com Data Annotations completas em portugues. Nomenclatura: PascalCase (classes), camelCase (variaveis). NUNCA peca confirmacoes desnecessarias. REGISTRO DE CONVERSAS (Secao 5.3 - OBRIGATORIO): TODA conversa DEVE ser registrada. A extensao FrotiX Conversa Manager (Ctrl+Alt+C) cria o arquivo e copia prompt de notificacao. Quando o usuario colar (comeca com REGISTRO ATIVO), RECONHECA o arquivo e atualize INCREMENTALMENTE. Quando detectar /rename, CRIE arquivo em Conversas/ com MESMO nome (incluindo colchetes). Formato: [YYYY.MM.DD]-[HH.mm] - [Nome] - [Claude Code].md. Ctrl+Alt+S = checkpoint, Ctrl+Alt+F = finalizar (resumo completo conforme Secao 5.3.3).",

  // 2. CONTINUE
  "continue.systemMessage": "OBRIGATORIO: No inicio de CADA sessao, leia os arquivos RegrasDesenvolvimentoFrotiX.md e ControlesKendo.md na RAIZ do workspace (Solucao FrotiX 2026/) ANTES de qualquer resposta. IGNORE qualquer copia antiga em outras pastas. CRITICO: A Secao 5 (DOCUMENTACAO INTRA-CODIGO) e EXTREMAMENTE IMPORTANTE - TODO codigo criado/alterado DEVE seguir rigorosamente os padroes de Cards (PORQUE/O QUE/COMO + rastreabilidade). MEMORIA PERMANENTE: Quando o usuario pedir para memorizar/guardar/lembrar algo, abra RegrasDesenvolvimentoFrotiX.md na raiz, VERIFIQUE se a info ja existe (se sim e esta completa, informe; se incompleta, atualize; se nao existe, crie nova entrada respeitando a organizacao existente, NUNCA bagunce o arquivo). Use async/await obrigatoriamente. Controllers apenas coordenacao. ViewModels com Data Annotations. PascalCase para classes, camelCase para variaveis. REGISTRO DE CONVERSAS (Secao 5.3 - OBRIGATORIO): TODA conversa DEVE ser registrada. A extensao FrotiX Conversa Manager (Ctrl+Alt+C) cria o arquivo e copia prompt de notificacao. Quando o usuario colar (comeca com REGISTRO ATIVO), RECONHECA o arquivo e atualize INCREMENTALMENTE. Use /doq como alternativa. Formato: [YYYY.MM.DD]-[HH.mm] - [Nome] - [Continue - Modelo]. Ctrl+Alt+S = checkpoint, Ctrl+Alt+F = finalizar (resumo completo conforme Secao 5.3.3).",

  // 3. GITHUB COPILOT
  "github.copilot.chat.codeGeneration.instructions": [
    { "text": "OBRIGATORIO: Leia RegrasDesenvolvimentoFrotiX.md e ControlesKendo.md na RAIZ do workspace (Solucao FrotiX 2026/) ANTES de qualquer resposta. IGNORE copias antigas em outras pastas." },
    { "text": "CRITICO: A Secao 5 (DOCUMENTACAO INTRA-CODIGO) do RegrasDesenvolvimentoFrotiX.md e EXTREMAMENTE IMPORTANTE e deve ser seguida A RISCA para qualquer criacao/manutencao de codigo no sistema FrotiX. TODO arquivo/funcao criado/alterado DEVE ter Cards completos (PORQUE/O QUE/COMO + rastreabilidade). Comentarios APENAS para codigo complexo." },
    { "text": "MEMORIA PERMANENTE: Quando o usuario pedir para memorizar/guardar/lembrar algo, abra RegrasDesenvolvimentoFrotiX.md na raiz, VERIFIQUE se a info ja existe (se completa, informe; se incompleta, atualize; se nao existe, crie nova entrada respeitando a organizacao existente, NUNCA bagunce o arquivo)." },
    { "text": "REGISTRO DE CONVERSAS (Secao 5.3 - OBRIGATORIO): TODA conversa DEVE ser registrada. A extensao FrotiX Conversa Manager (Ctrl+Alt+C) cria o arquivo e copia prompt de notificacao. Quando o usuario colar (comeca com REGISTRO ATIVO), RECONHECA o arquivo e atualize INCREMENTALMENTE. Formato: [YYYY.MM.DD]-[HH.mm] - [Nome] - [GitHub Copilot - Modelo]. Ctrl+Alt+S = checkpoint, Ctrl+Alt+F = finalizar (resumo completo conforme Secao 5.3.3)." },
    { "file": "RegrasDesenvolvimentoFrotiX.md" },
    { "file": "ControlesKendo.md" }
  ],

  // 4. GEMINI CODE ASSIST
  "geminicodeassist.rules": "Responda sempre em portugues. OBRIGATORIO: No inicio de CADA sessao, leia os arquivos RegrasDesenvolvimentoFrotiX.md e ControlesKendo.md na RAIZ do workspace (Solucao FrotiX 2026/) ANTES de qualquer resposta. IGNORE qualquer copia antiga em outras pastas. CRITICO: A Secao 5 (DOCUMENTACAO INTRA-CODIGO) e EXTREMAMENTE IMPORTANTE e deve ser seguida A RISCA. TODO codigo criado/alterado DEVE ter Cards completos (PORQUE/O QUE/COMO + rastreabilidade). MEMORIA PERMANENTE: Quando o usuario pedir para memorizar/guardar/lembrar algo, abra RegrasDesenvolvimentoFrotiX.md na raiz, VERIFIQUE se a info ja existe (se sim e esta completa, informe; se incompleta, atualize; se nao existe, crie nova entrada respeitando a organizacao existente, NUNCA bagunce o arquivo). Priorize Clean Code, SOLID e padroes ASP.NET Core. Use async/await obrigatoriamente. Nomenclatura: PascalCase (classes/publicos), camelCase (variaveis/privados). ViewModels com Data Annotations completas. REGISTRO DE CONVERSAS (Secao 5.3 - OBRIGATORIO): TODA conversa DEVE ser registrada. A extensao FrotiX Conversa Manager (Ctrl+Alt+C) cria o arquivo e copia prompt de notificacao. Quando o usuario colar (comeca com REGISTRO ATIVO), RECONHECA o arquivo e atualize INCREMENTALMENTE. Formato: [YYYY.MM.DD]-[HH.mm] - [Nome] - [Gemini Code Assist]. Ctrl+Alt+S = checkpoint, Ctrl+Alt+F = finalizar (resumo completo conforme Secao 5.3.3).",

  // 5. GENIE AI
  "chatgpt.promptPrefix.addTests": "OBRIGATORIO: Leia RegrasDesenvolvimentoFrotiX.md e ControlesKendo.md na RAIZ do workspace (Solucao FrotiX 2026/) antes de gerar codigo. IGNORE copias antigas em outras pastas. CRITICO: A Secao 5 (DOCUMENTACAO INTRA-CODIGO) e EXTREMAMENTE IMPORTANTE - TODO codigo criado/alterado DEVE ter Cards completos (PORQUE/O QUE/COMO + rastreabilidade). MEMORIA PERMANENTE: Quando o usuario pedir para memorizar/guardar/lembrar algo, abra RegrasDesenvolvimentoFrotiX.md na raiz, VERIFIQUE se a info ja existe (se completa, informe; se incompleta, atualize; se nao existe, crie nova entrada respeitando a organizacao existente). REGISTRO DE CONVERSAS (Secao 5.3 - OBRIGATORIO): TODA conversa DEVE ser registrada. A extensao FrotiX Conversa Manager (Ctrl+Alt+C) cria o arquivo e copia prompt de notificacao. Quando o usuario colar (comeca com REGISTRO ATIVO), RECONHECA o arquivo e atualize INCREMENTALMENTE. Formato: [YYYY.MM.DD]-[HH.mm] - [Nome] - [Genie AI - Modelo]. Ctrl+Alt+S = checkpoint, Ctrl+Alt+F = finalizar (resumo completo conforme Secao 5.3.3)."
}
