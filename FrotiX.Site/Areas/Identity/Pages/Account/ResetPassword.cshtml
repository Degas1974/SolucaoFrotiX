@*
╔════════════════════════════════════════════════════════════════════════════════════════════════════╗
║ 📌 ARQUIVO: ResetPassword.cshtml                                                                    ║
║ 📂 CAMINHO: /Areas/Identity/Pages/Account                                                           ║
╠════════════════════════════════════════════════════════════════════════════════════════════════════╣
║ 🧭 OBJETIVO: Página de redefinição de senha com token de recuperação.                               ║
╚════════════════════════════════════════════════════════════════════════════════════════════════════╝
*@

@page
@model ResetPasswordModel
@{
    ViewData["Title"] = "Reset password";
}

@* [DOC] Títulos da página *@
<h1>@ViewData["Title"]</h1>
<h4>Reset your password.</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @* [DOC] Formulário de reset de senha *@
        <form method="post">
            @* [DOC] Sumário de erros de validação *@
            <div asp-validation-summary="All" class="text-danger"></div>
            @* [DOC] Código de recuperação (hidden) *@
            <input asp-for="Input.Code" type="hidden" />
            <div class="form-group">
                @* [DOC] Campo de email do usuário *@
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                @* [DOC] Campo de nova senha *@
                <label asp-for="Input.Password"></label>
                <input asp-for="Input.Password" class="form-control" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                @* [DOC] Campo de confirmação de senha *@
                <label asp-for="Input.ConfirmPassword"></label>
                <input asp-for="Input.ConfirmPassword" class="form-control" />
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-azul">Reset</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
