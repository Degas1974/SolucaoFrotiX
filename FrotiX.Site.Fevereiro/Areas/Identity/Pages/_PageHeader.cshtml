@*****************************************************************************************
 * ⚡ ARQUIVO: _PageHeader.cshtml
 * --------------------------------------------------------------------------------------
 * 🎯 OBJETIVO     : Partial de cabeçalho da área Identity, exibindo logo e link de ação
 *                   contextual (Login ou Register) conforme a página atual.
 *
 * 📥 ENTRADAS     : ViewBag.PageName (string) para decidir qual link exibir.
 *
 * 📤 SAÍDAS       : HTML com layout do header e links de navegação.
 *
 * 🔗 CHAMADA POR  : Layouts da área Identity (ex: _Layout.cshtml).
 *
 * 🔄 CHAMA        : Partial _Logo, páginas /account/login e /account/register.
 *
 * 📦 DEPENDÊNCIAS : Razor Pages, Tag Helpers (asp-area/asp-page), Bootstrap.
 *
 * 📝 OBSERVAÇÕES  : Lista de páginas (register, forgotpassword, lockout, logout) define
 *                   quando mostrar "Secure Login".
 *****************************************************************************************@
@{
    // [DOC] Captura nome da página atual do ViewBag para determinar contexto
    var pageName = ViewBag.PageName ?? string.Empty;

    // [DOC] Define se deve exibir link de Login (páginas register, forgotpassword, lockout, logout)
    var canLogin = new string[] { "register", "forgotpassword", "lockout", "logout" }.ToList().Contains(pageName);
}
@* [DOC] Container do cabeçalho com gradiente da marca, sombra e altura fixa (height-10) *@
<div class="height-10 w-100 shadow-lg px-4 bg-brand-gradient">
    <div class="d-flex align-items-center container p-0">
        @* [DOC] Renderiza logo do sistema (partial _Logo) *@
        <partial name="_Logo" />
        @* [DOC] Lógica condicional: se página permite login, mostra link "Secure Login" *@
        @if (canLogin)
        {
            <span class="text-white opacity-50 ml-auto mr-2 hidden-sm-down">
                Already a member?
            </span>
            <a class="btn-link text-white ml-auto ml-sm-0" asp-area="Identity" asp-page="/account/login">
                Secure Login
            </a>
        }
        else
        {
            @* [DOC] Caso contrário, mostra link "Register account" *@
            <a class="btn-link text-white ml-auto" asp-area="Identity" asp-page="/account/register">
                Register account
            </a>
        }
    </div>
</div>
