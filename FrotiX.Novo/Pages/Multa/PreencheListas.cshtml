@*
* ⚡ ARQUIVO: Pages/Multa/PreencheListas.cshtml
* ================================================================================================
* 🎯 OBJETIVO     : Página Razor VAZIA servindo como endpoint para requisições AJAX do PageModel.
*                   Lógica centralizada em PreencheListasModel.OnGet* handlers (code-behind).
*                   Usado para popular dropdowns/listas via AJAX sem renderizar HTML.
*
* 📥 ENTRADAS     : Requisições AJAX GET/POST para /Multa/PreencheListas?handler=NomeHandler,
*                   parâmetros via query string ou form data
*
* 📤 SAÍDAS       : JSON/data response do code-behind (PreencheListasModel handlers),
*                   sem renderização HTML (apenas dados estruturados)
*
* 🔗 CHAMADA POR  : JavaScript em Pages/Multa (Multa/Upsert.cshtml, multa.js, etc),
*                   $.ajax() ou fetch() para carregar dados dinamicamente
*
* 🔄 CHAMA        : PreencheListasModel.OnGet* handlers (code-behind PreencheListas.cshtml.cs),
*                   Repository/Service layer (acesso ao banco de dados),
*                   return JsonResult(data) para resposta estruturada
*
* 📦 DEPENDÊNCIAS : ASP.NET Core Razor Pages (PageModel), PreencheListasModel (C# code-behind),
*                   Repository layer (dados de multas), JavaScript caller (AJAX)
*
* 📝 OBSERVAÇÕES  : 27 linhas (após card). Página PADRÃO para AJAX endpoints - sem conteúdo
*                   renderizável, apenas @page + @model + bloco vazio. Toda lógica em
*                   PreencheListasModel.cs. Típicos handlers:
*                   • /Multa/PreencheListas?handler=Veiculos → Array<VeiculoDto>
*                   • /Multa/PreencheListas?handler=Motoristas → Array<MotoristaDto>
*                   • /Multa/PreencheListas?handler=TiposMulta → Array<TipoMultaDto>
*
* ================================================================================================ *@
@page
@model FrotiX.Pages.Multa.PreencheListasModel
@{
}
