@* ****************************************************************************************
 * ⚡ ARQUIVO: Pages/Contrato/Upsert.cshtml
 * --------------------------------------------------------------------------------------
 * 🎯 OBJETIVO     : Formulário completo criar/editar contrato com dados, itens, empenhos, anexos, repactuações
 * 📥 ENTRADAS     : @page, @model UpsertModel, campos contrato (número, objeto, vigência, valores), grids (itens, empenhos), uploads anexos
 * 📤 SAÍDAS       : POST /api/Contrato/Insere ou Edita (JSON), redirect /Contrato, modais (itens, empenhos, anexos), AppToast feedback
 * 🔗 CHAMADA POR  : Contrato/Index (botão "Adicionar Contrato", editar linha)
 * 🔄 CHAMA        : /api/Contrato/Insere, /api/Contrato/Edita, /api/Contrato/SalvarItem, /api/Contrato/SalvarEmpenho, /api/Contrato/UploadAnexo
 * 📦 DEPENDÊNCIAS : Syncfusion EJ2 (DatePicker, ComboBox, Grid, Upload), Bootstrap 5, jQuery, DataTables, moeda() inline
 * 📝 OBSERVAÇÕES  : Total: 1595 linhas. PROBLEMAS: Arquivo MASSIVO, CSS inline ~400 linhas, JavaScript inline ~600 linhas, múltiplos grids e modais, função moeda() duplicada, validações fracas, sem lazy loading modais, double-submit possível
 **************************************************************************************** *@
@page
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@model FrotiX.Pages.Contrato.UpsertModel

@{
    ViewData["Title"] = "Contratos";
    ViewData["PageName"] = "contrato_upsert";
    ViewData["Heading"] = "<i class='fa-duotone fa-folders'></i> Cadastros: <span class='fw-300'>Contratos</span>";
    ViewData["Category1"] = "Cadastros";
    ViewData["PageIcon"] = "fa-duotone fa-folders";

    var isEdit = Model.ContratoObj.Contrato.ContratoId != Guid.Empty;
    var tituloCard = isEdit ? "Atualizar Contrato" : "Novo Contrato";
}

<style>
    /* ===== CARD FROTIX - CONTRATOS ===== */
    /* Header usa o padrão global .ftx-card-header do frotix.css */

    .ftx-card-contrato {
        border: none;
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
        background: #fff;
        margin-bottom: 1.5rem;
    }

    .ftx-card-contrato .ftx-card-body {
        padding: 1.5rem;
        background: linear-gradient(180deg, #fafafa 0%, #ffffff 100%);
    }

    /* ===== SEÇÕES DO FORMULÁRIO ===== */
    .ftx-form-section {
        background: #fff;
        border-radius: 10px;
        padding: 1.25rem;
        margin-bottom: 1rem;
        border: 1px solid rgba(50, 93, 136, 0.1);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    }

    .ftx-form-section-title {
        font-family: 'Outfit', sans-serif;
        font-weight: 600;
        font-size: 1rem;
        color: #325d88;
        margin-bottom: 1rem;
        padding-bottom: 0.5rem;
        border-bottom: 2px solid rgba(50, 93, 136, 0.15);
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .ftx-form-section-title i {
        font-size: 1.1rem;
        --fa-primary-color: #325d88;
        --fa-secondary-color: #6b9bc3;
        --fa-secondary-opacity: 0.7;
    }

    /* ===== LABELS E INPUTS ===== */
    .label {
        margin-bottom: -5px;
        margin-top: 10px;
        font-weight: 600;
        color: #2d3748;
        font-size: 0.85rem;
    }

    .form-control-xs {
        height: calc(1em + .775rem + 2px) !important;
        padding: .125rem .5rem !important;
        font-size: .85rem !important;
        line-height: 1.5;
        border-radius: .35rem;
        border: 1px solid #cbd5e0;
        transition: all 0.2s ease;
    }

    .form-control-xs:focus {
        border-color: #325d88;
        box-shadow: 0 0 0 3px rgba(50, 93, 136, 0.15);
    }

    /* ===== CHECKBOX ESTILIZADO ===== */
    input[type=checkbox] {
        vertical-align: middle;
        position: relative;
        bottom: 1px;
        accent-color: #325d88;
    }

    /* ===== BOTÕES DE AÇÃO ===== */
    .ftx-btn-action {
        border-radius: 8px;
        padding: 0.65rem 1.5rem;
        font-family: 'Outfit', sans-serif;
        font-weight: 600;
        font-size: 0.95rem;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        transition: all 0.3s ease;
        border: none;
        min-height: 44px;
    }

    .ftx-btn-action i {
        font-size: 1rem;
    }

    .ftx-btn-action.btn-azul {
        background: linear-gradient(135deg, #325d88 0%, #3d6f9e 100%);
        color: #fff;
        box-shadow: 0 4px 12px rgba(50, 93, 136, 0.3);
    }

    .ftx-btn-action.btn-azul:hover {
        background: linear-gradient(135deg, #2a4f75 0%, #325d88 100%);
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(50, 93, 136, 0.4);
    }

    .ftx-btn-action.btn-vinho {
        background: linear-gradient(135deg, #722F37 0%, #8B3A44 100%);
        color: #fff;
        box-shadow: 0 4px 12px rgba(114, 47, 55, 0.3);
    }

    .ftx-btn-action.btn-vinho:hover {
        background: linear-gradient(135deg, #5a252c 0%, #722F37 100%);
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(114, 47, 55, 0.4);
    }

    /* Gap para flex containers */
    .gap-2 {
        gap: 0.75rem !important;
    }

    /* Seção de Ações (última seção) */
    .ftx-form-section:last-of-type {
        background: linear-gradient(180deg, #f8fafc 0%, #fff 100%);
        border: 1px solid rgba(50, 93, 136, 0.15);
        margin-bottom: 0;
    }

    /* ===== GRID SYNCFUSION ===== */
    .e-grid * {
        font-size: 12px !important;
    }

    .disable-element {
        pointer-events: none;
        opacity: 0.4;
    }

    .wrapper {
        cursor: not-allowed;
    }

    /* Grid dentro das seções */
    .ftx-form-section .e-grid {
        border-radius: 8px;
        overflow: hidden;
        border: 1px solid #e2e8f0;
    }

    .ftx-form-section .e-grid .e-toolbar {
        background: #f8fafc;
        border-bottom: 1px solid #e2e8f0;
    }

    /* Seção de Veículos quando visível */
    #divVeiculosAdd.ftx-form-section,
    #divVeiculosEdit.ftx-form-section {
        margin-top: 0;
    }

    /* Linha do Total e botão Atualizar */
    #divVeiculosAdd .row.mt-3,
    #divVeiculosEdit .row.mt-3 {
        align-items: flex-end;
        margin-top: 1rem !important;
    }

    /* ===== TERCEIRIZAÇÃO SECTION ===== */
    .ftx-terceirizacao-card {
        background: linear-gradient(135deg, #f8fafc 0%, #edf2f7 100%);
        border-radius: 10px;
        padding: 1rem;
        border: 1px solid #e2e8f0;
    }

    .ftx-terceirizacao-card label {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    /* ===== RESPONSIVIDADE ===== */
    @@media (max-width: 768px) {
        .ftx-card-contrato .ftx-card-body {
            padding: 1rem;
        }
    }
</style>

@section HeadBlock {
    <style>
        /* ======== OUTLINE BRANCO NO BOTÃO DO HEADER (Padrão FrotiX) ======== */
        .ftx-card-header .btn-fundo-laranja {
            outline: 2px solid rgba(255, 255, 255, 0.5) !important;
            outline-offset: 1px;
            transition: all 0.2s ease;
        }

        .ftx-card-header .btn-fundo-laranja:hover {
            outline: 2px solid rgba(255, 255, 255, 0.8) !important;
            outline-offset: 2px;
        }
    </style>
}

<form method="post" asp-action="Upsert">
    <div class="row">
        <div class="col-xl-12">
            <!-- Card FrotiX -->
            <div class="ftx-card-contrato">
                <!-- Header do Card - Usa padrão global ftx-card-header -->
                <div class="ftx-card-header">
                    <h2 class="titulo-paginas">
                        <i class="fa-duotone fa-file-contract"></i>
                        @tituloCard
                    </h2>
                    <div class="ftx-card-actions">
                        <a asp-page="./Index" class="btn btn-fundo-laranja" data-ftx-loading>
                            <i class="fa-duotone fa-rotate-left icon-space icon-rotate-left"></i>
                            Voltar à Lista
                        </a>
                    </div>
                </div>

                <!-- Body do Card -->
                <div class="ftx-card-body">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    @if (isEdit)
                    {
                        <input type="hidden" asp-for="ContratoObj.Contrato.ContratoId" />
                    }

                    <!-- Seção: Identificação do Contrato -->
                    <div class="ftx-form-section">
                        <div class="ftx-form-section-title">
                            <i class="fa-duotone fa-id-card"></i>
                            Identificação do Contrato
                        </div>
                        <div class="row">
                            <div class="col-md-2 col-sm-6">
                                <div class="form-group">
                                    <label class="label label-required"
                                        asp-for="ContratoObj.Contrato.AnoContrato"></label>
                                    <span class="text-danger"
                                        asp-validation-for="ContratoObj.Contrato.AnoContrato"></span>
                                    <input id="txtAno" class="form-control form-control-xs"
                                        asp-for="ContratoObj.Contrato.AnoContrato" placeholder="Ex: 2024" />
                                </div>
                            </div>
                            <div class="col-md-2 col-sm-6">
                                <div class="form-group">
                                    <label class="label label-required"
                                        asp-for="ContratoObj.Contrato.NumeroContrato"></label>
                                    <span class="text-danger"
                                        asp-validation-for="ContratoObj.Contrato.NumeroContrato"></span>
                                    <input id="txtNumeroContrato" class="form-control form-control-xs"
                                        asp-for="ContratoObj.Contrato.NumeroContrato" placeholder="Ex: 001" />
                                </div>
                            </div>
                            <div class="col-md-2 col-sm-6">
                                <div class="form-group">
                                    <label class="label label-required"
                                        asp-for="ContratoObj.Contrato.NumeroProcesso"></label>
                                    <span class="text-danger"
                                        asp-validation-for="ContratoObj.Contrato.NumeroProcesso"></span>
                                    <input id="txtNumeroProcesso" class="form-control form-control-xs"
                                        asp-for="ContratoObj.Contrato.NumeroProcesso" placeholder="Ex: 12345" />
                                </div>
                            </div>
                            <div class="col-md-2 col-sm-6">
                                <div class="form-group">
                                    <label class="label label-required"
                                        asp-for="ContratoObj.Contrato.AnoProcesso"></label>
                                    <span class="text-danger"
                                        asp-validation-for="ContratoObj.Contrato.AnoProcesso"></span>
                                    <input id="txtAnoProcesso" class="form-control form-control-xs"
                                        asp-for="ContratoObj.Contrato.AnoProcesso" placeholder="Ex: 2024" />
                                </div>
                            </div>
                            <div class="col-md-2 col-sm-6">
                                <div class="form-group">
                                    <label class="label label-required" asp-for="ContratoObj.Contrato.Vigencia"></label>
                                    <span class="text-danger" asp-validation-for="ContratoObj.Contrato.Vigencia"></span>
                                    <select id="lstVigencia" class="form-control form-control-xs"
                                        asp-for="ContratoObj.Contrato.Vigencia">
                                        <option value="1">1</option>
                                        <option value="2">2</option>
                                        <option value="3">3</option>
                                        <option value="4">4</option>
                                        <option value="5">5</option>
                                        <option value="6">6</option>
                                        <option value="7">7</option>
                                        <option value="8">8</option>
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-2 col-sm-6">
                                <div class="form-group">
                                    <label class="label" asp-for="ContratoObj.Contrato.Prorrogacao"></label>
                                    <span class="text-danger"
                                        asp-validation-for="ContratoObj.Contrato.Prorrogacao"></span>
                                    <select id="lstProrrogacao" class="form-control form-control-xs"
                                        asp-for="ContratoObj.Contrato.Prorrogacao">
                                        <option value="1">1</option>
                                        <option value="2">2</option>
                                        <option value="3">3</option>
                                        <option value="4">4</option>
                                        <option value="5">5</option>
                                        <option value="6">6</option>
                                        <option value="7">7</option>
                                        <option value="8">8</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Seção: Fornecedor e Objeto -->
                    <div class="ftx-form-section">
                        <div class="ftx-form-section-title">
                            <i class="fa-duotone fa-building"></i>
                            Fornecedor e Objeto
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label class="label label-required"
                                        asp-for="ContratoObj.Contrato.FornecedorId"></label>
                                    <span class="text-danger"
                                        asp-validation-for="ContratoObj.Contrato.FornecedorId"></span>
                                    @Html.DropDownListFor(m => m.ContratoObj.Contrato.FornecedorId,
                                    Model.ContratoObj.FornecedorList,
                                                                        "- Selecione um Fornecedor -",
                                                                        new { @class = "form-control form-control-xs", @Name = "lstFornecedor" })
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label class="label label-required" asp-for="ContratoObj.Contrato.Objeto"></label>
                                    <span class="text-danger" asp-validation-for="ContratoObj.Contrato.Objeto"></span>
                                    <input id="lstObjeto" class="form-control form-control-xs"
                                        asp-for="ContratoObj.Contrato.Objeto"
                                        placeholder="Descrição do objeto do contrato" />
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Seção: Detalhes do Contrato -->
                    <div class="ftx-form-section">
                        <div class="ftx-form-section-title">
                            <i class="fa-duotone fa-calendar-lines-pen"></i>
                            Detalhes do Contrato
                        </div>
                        <div class="row">
                            <div id="divTipoContrato" class="col-md-3 col-sm-6">
                                <div class="form-group">
                                    <label class="label label-required"
                                        asp-for="ContratoObj.Contrato.TipoContrato"></label>
                                    <span class="text-danger"
                                        asp-validation-for="ContratoObj.Contrato.TipoContrato"></span>
                                    <select id="lstTipoContrato" class="form-control form-control-xs"
                                        asp-for="ContratoObj.Contrato.TipoContrato">
                                        <option value="">-- Tipo Contrato --</option>
                                        <option value="Serviços">Serviços</option>
                                        <option value="Terceirização">Terceirização</option>
                                        <option value="Locação">Locação</option>
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-3 col-sm-6">
                                <div class="form-group">
                                    <label class="label label-required"
                                        asp-for="ContratoObj.Contrato.DataInicio"></label>
                                    <span class="text-danger"
                                        asp-validation-for="ContratoObj.Contrato.DataInicio"></span>
                                    <input id="txtDataInicio" class="form-control form-control-xs" type="date"
                                        asp-for="ContratoObj.Contrato.DataInicio" />
                                </div>
                            </div>
                            <div class="col-md-3 col-sm-6">
                                <div class="form-group">
                                    <label class="label" asp-for="ContratoObj.Contrato.DataFim"></label>
                                    <span class="text-danger" asp-validation-for="ContratoObj.Contrato.DataFim"></span>
                                    <input id="txtDataFim" class="form-control form-control-xs" type="date"
                                        asp-for="ContratoObj.Contrato.DataFim" />
                                </div>
                            </div>
                            <div class="col-md-3 col-sm-6">
                                <div class="form-group">
                                    <label class="label" id="lblValor">Valor (R$)</label>
                                    <span class="text-danger" asp-validation-for="ContratoObj.Contrato.Valor"></span>
                                    <input id="valor" class="form-control form-control-xs"
                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                        asp-for="ContratoObj.Contrato.Valor"
                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                </div>
                            </div>
                        </div>

                        <!-- Lista de Repactuações (exibido apenas em edição) -->
                        <div id="divListaRepactuacoes" class="row mt-3" style="display:none">
                            <div class="col-md-6">
                                <label class="label">Selecione uma Repactuação para ver os Itens</label>
                                <select id="lstRepactuacao" name="lstRepactuacao" class="form-control form-control-xs">
                                    <option value="">-- Selecione uma Repactuação --</option>
                                </select>
                            </div>
                        </div>
                    </div>

                    <!-- Seção: Terceirização (condicional) -->
                    <div id="divTerceirizacao" class="ftx-form-section" style="display:none">
                        <div class="ftx-form-section-title">
                            <i class="fa-duotone fa-users-gear"></i>
                            Configuração de Terceirização
                        </div>
                        <div class="row">
                            <!-- Encarregados -->
                            <div class="col-md-3">
                                <div class="ftx-terceirizacao-card">
                                    @if (!isEdit)
                                    {
                                        <label id="lblencarregados" class="label">
                                            <div>
                                                <input id="chkencarregados" type="checkbox" class="form-check-input"
                                                    asp-for="ContratoObj.Contrato.ContratoEncarregados" />
                                                <strong>Encarregados</strong>
                                            </div>
                                            <div class="row mt-2">
                                                <div class="col-8">
                                                    <small class="text-muted">Custo Mensal (R$)</small>
                                                    <input id="CustoMensalEncarregados" disabled
                                                        class="form-control form-control-xs"
                                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                                        asp-for="ContratoObj.Contrato.CustoMensalEncarregado"
                                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                                </div>
                                                <div class="col-4">
                                                    <small class="text-muted">Qtd.</small>
                                                    <input id="txtQtdEncarregados" disabled
                                                        class="form-control form-control-xs"
                                                        asp-for="ContratoObj.Contrato.QuantidadeEncarregado"
                                                        style="text-align: right;" />
                                                </div>
                                            </div>
                                        </label>
                                    }
                                    else if (Model.ContratoObj.Contrato.TipoContrato == "Terceirização")
                                    {
                                        <label id="lblencarregados" class="label">
                                            <div>
                                                <input disabled id="chkencarregados" type="checkbox"
                                                    class="form-check-input" />
                                                <strong>Encarregados</strong>
                                            </div>
                                            <div class="row mt-2">
                                                <div class="col-8">
                                                    <small class="text-muted">Custo Mensal (R$)</small>
                                                    <input disabled id="CustoMensalEncarregados"
                                                        class="form-control form-control-xs"
                                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                                </div>
                                                <div class="col-4">
                                                    <small class="text-muted">Qtd.</small>
                                                    <input disabled id="txtQtdEncarregados"
                                                        class="form-control form-control-xs" style="text-align: right;" />
                                                </div>
                                            </div>
                                        </label>
                                    }
                                </div>
                            </div>

                            <!-- Operadores -->
                            <div class="col-md-3">
                                <div class="ftx-terceirizacao-card">
                                    @if (!isEdit)
                                    {
                                        <label id="lbloperadores" class="label">
                                            <div>
                                                <input disabled id="chkoperadores" type="checkbox" class="form-check-input"
                                                    asp-for="ContratoObj.Contrato.ContratoOperadores" />
                                                <strong>Operadores</strong>
                                            </div>
                                            <div class="row mt-2">
                                                <div class="col-8">
                                                    <small class="text-muted">Custo Mensal (R$)</small>
                                                    <input id="CustoMensalOperadores" disabled
                                                        class="form-control form-control-xs"
                                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                                        asp-for="ContratoObj.Contrato.CustoMensalOperador"
                                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                                </div>
                                                <div class="col-4">
                                                    <small class="text-muted">Qtd.</small>
                                                    <input id="txtQtdOperadores" disabled
                                                        class="form-control form-control-xs"
                                                        asp-for="ContratoObj.Contrato.QuantidadeOperador"
                                                        style="text-align: right;" />
                                                </div>
                                            </div>
                                        </label>
                                    }
                                    else if (Model.ContratoObj.Contrato.TipoContrato == "Terceirização")
                                    {
                                        <label id="lbloperadores" class="label">
                                            <div>
                                                <input disabled id="chkoperadores" type="checkbox"
                                                    class="form-check-input" />
                                                <strong>Operadores</strong>
                                            </div>
                                            <div class="row mt-2">
                                                <div class="col-8">
                                                    <small class="text-muted">Custo Mensal (R$)</small>
                                                    <input disabled id="CustoMensalOperadores"
                                                        class="form-control form-control-xs"
                                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                                </div>
                                                <div class="col-4">
                                                    <small class="text-muted">Qtd.</small>
                                                    <input disabled id="txtQtdOperadores"
                                                        class="form-control form-control-xs" style="text-align: right;" />
                                                </div>
                                            </div>
                                        </label>
                                    }
                                </div>
                            </div>

                            <!-- Motoristas -->
                            <div class="col-md-3">
                                <div class="ftx-terceirizacao-card">
                                    @if (!isEdit)
                                    {
                                        <label id="lblmotoristas" class="label">
                                            <div>
                                                <input disabled id="chkmotoristas" type="checkbox" class="form-check-input"
                                                    asp-for="ContratoObj.Contrato.ContratoMotoristas" />
                                                <strong>Motoristas</strong>
                                            </div>
                                            <div class="row mt-2">
                                                <div class="col-8">
                                                    <small class="text-muted">Custo Mensal (R$)</small>
                                                    <input disabled id="CustoMensalMotoristas"
                                                        class="form-control form-control-xs"
                                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                                        asp-for="ContratoObj.Contrato.CustoMensalMotorista"
                                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                                </div>
                                                <div class="col-4">
                                                    <small class="text-muted">Qtd.</small>
                                                    <input disabled id="txtQtdMotoristas"
                                                        class="form-control form-control-xs"
                                                        asp-for="ContratoObj.Contrato.QuantidadeMotorista"
                                                        style="text-align: right;" />
                                                </div>
                                            </div>
                                        </label>
                                    }
                                    else if (Model.ContratoObj.Contrato.TipoContrato == "Terceirização")
                                    {
                                        <label id="lblmotoristas" class="label">
                                            <div>
                                                <input disabled id="chkmotoristas" type="checkbox"
                                                    class="form-check-input" />
                                                <strong>Motoristas</strong>
                                            </div>
                                            <div class="row mt-2">
                                                <div class="col-8">
                                                    <small class="text-muted">Custo Mensal (R$)</small>
                                                    <input disabled id="CustoMensalMotoristas"
                                                        class="form-control form-control-xs"
                                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                                </div>
                                                <div class="col-4">
                                                    <small class="text-muted">Qtd.</small>
                                                    <input disabled id="txtQtdMotoristas"
                                                        class="form-control form-control-xs" style="text-align: right;" />
                                                </div>
                                            </div>
                                        </label>
                                    }
                                </div>
                            </div>

                            <!-- Lavadores -->
                            <div class="col-md-3">
                                <div class="ftx-terceirizacao-card">
                                    @if (!isEdit)
                                    {
                                        <label id="lbllavadores" class="label">
                                            <div>
                                                <input id="chklavadores" type="checkbox" class="form-check-input"
                                                    asp-for="ContratoObj.Contrato.ContratoLavadores" />
                                                <strong>Lavadores</strong>
                                            </div>
                                            <div class="row mt-2">
                                                <div class="col-8">
                                                    <small class="text-muted">Custo Mensal (R$)</small>
                                                    <input id="CustoMensalLavadores" disabled
                                                        class="form-control form-control-xs"
                                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                                        asp-for="ContratoObj.Contrato.CustoMensalLavador"
                                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                                </div>
                                                <div class="col-4">
                                                    <small class="text-muted">Qtd.</small>
                                                    <input id="txtQtdLavadores" disabled
                                                        class="form-control form-control-xs"
                                                        asp-for="ContratoObj.Contrato.QuantidadeLavador"
                                                        style="text-align: right;" />
                                                </div>
                                            </div>
                                        </label>
                                    }
                                    else if (Model.ContratoObj.Contrato.TipoContrato == "Terceirização")
                                    {
                                        <label id="lbllavadores" class="label">
                                            <div>
                                                <input disabled id="chklavadores" type="checkbox"
                                                    class="form-check-input" />
                                                <strong>Lavadores</strong>
                                            </div>
                                            <div class="row mt-2">
                                                <div class="col-8">
                                                    <small class="text-muted">Custo Mensal (R$)</small>
                                                    <input disabled id="CustoMensalLavadores"
                                                        class="form-control form-control-xs"
                                                        data-inputmask="'alias': 'currency'" style="text-align: right;"
                                                        onKeyPress="return(moeda(this,'.',',',event))" />
                                                </div>
                                                <div class="col-4">
                                                    <small class="text-muted">Qtd.</small>
                                                    <input disabled id="txtQtdLavadores"
                                                        class="form-control form-control-xs" style="text-align: right;" />
                                                </div>
                                            </div>
                                        </label>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Seção: Veículos (Locação - Novo) -->
                    <div id="divVeiculosAdd" class="ftx-form-section" style="display:none">
                        <div class="ftx-form-section-title">
                            <i class="fa-duotone fa-cars"></i>
                            Veículos do Contrato
                        </div>
                        @{
                            List<object> cols = new List<object>();
                            cols.Add(new { field = "numitem", direction = "Ascending" });
                        }
                        <ejs-grid id="grdVeiculos"
                            toolbar="@(new List<string>() { "Add", "Update", "Delete", "Cancel" })" GridLines="Both"
                            QueryCellInfo="calculate" allowSorting="true">
                            <e-grid-editSettings allowAdding="true" allowDeleting="true" allowEditing="true"
                                newRowPosition="Bottom"></e-grid-editSettings>
                            <e-grid-sortsettings columns="cols"></e-grid-sortsettings>
                            <e-grid-columns>
                                <e-grid-column field="numitem" headerText="Item" textAlign="Center" width="20"
                                    allowEditing="true"></e-grid-column>
                                <e-grid-column field="descricao" headerText="Descrição do Veículo" textAlign="Left"
                                    width="150" allowEditing="true"></e-grid-column>
                                <e-grid-column field="quantidade" headerText="Quantidade" textAlign="Center" width="30"
                                    allowEditing="true"></e-grid-column>
                                <e-grid-column field="valorunitario" headerText="Valor Unitário" textAlign="Right"
                                    width="30" allowEditing="true" format="N2"></e-grid-column>
                                <e-grid-column field="valortotal" headerText="Valor Total" width="30" textAlign="Right"
                                    allowEditing="false" format="N2"></e-grid-column>
                            </e-grid-columns>
                        </ejs-grid>
                        <div class="row mt-3 align-items-end">
                            <div class="col-md-3 col-sm-6">
                                <label class="label">Total Geral</label>
                                <input id="txtTotal" class="form-control form-control-xs"
                                    data-inputmask="'alias': 'currency'" style="text-align: right;" disabled />
                            </div>
                            <div class="col-md-3 col-sm-6">
                                <button id="btnAtualizaTotal" type="button" class="btn ftx-btn-action btn-azul">
                                    <i class="fa-duotone fa-arrows-rotate"></i> Atualizar Total
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Seção: Veículos (Locação - Edição) -->
                    <div id="divVeiculosEdit" class="ftx-form-section" style="display:none">
                        <div class="ftx-form-section-title">
                            <i class="fa-duotone fa-cars"></i>
                            Veículos do Contrato
                        </div>
                        <ejs-grid id="grdVeiculos2" allowPaging="false" GridLines="Both">
                            <e-grid-columns>
                                <e-grid-column field="numitem" headerText="Item" textAlign="Center"
                                    width="20"></e-grid-column>
                                <e-grid-column field="descricao" headerText="Descrição do Veículo" textAlign="Left"
                                    width="150"></e-grid-column>
                                <e-grid-column field="quantidade" headerText="Quantidade" textAlign="Center" width="30"
                                    format="N0"></e-grid-column>
                                <e-grid-column field="valorunitario" headerText="Valor Unitário" textAlign="Right"
                                    width="30" format="N2"></e-grid-column>
                                <e-grid-column field="valortotal" headerText="Valor Total" width="30" textAlign="Right"
                                    format="N2"></e-grid-column>
                            </e-grid-columns>
                        </ejs-grid>
                        <div class="row mt-3">
                            <div class="col-md-3">
                                <label class="label">Total Geral</label>
                                <input id="txtTotalEdit" class="form-control form-control-xs"
                                    data-inputmask="'alias': 'currency'" style="text-align: right;" disabled />
                            </div>
                        </div>
                    </div>

                    <!-- Seção: Status e Ações -->
                    <div class="ftx-form-section">
                        <div class="row align-items-center">
                            <div class="col-md-6 col-sm-12 mb-2 mb-md-0">
                                <label class="label d-flex align-items-center gap-2 mb-0">
                                    <input id="chkAtivo" type="checkbox" class="form-check-input"
                                        asp-for="ContratoObj.Contrato.Status"
                                        style="width: 18px; height: 18px; margin: 0;" />
                                    <span style="font-size: 0.95rem;">Contrato Ativo</span>
                                </label>
                            </div>
                            <div class="col-md-6 col-sm-12">
                                <div class="d-flex justify-content-end gap-2 flex-wrap">
                                    @if (isEdit)
                                    {
                                        <button id="btnEdita" type="button" asp-page-handler="Edit"
                                            asp-route-id="@Model.ContratoObj.Contrato.ContratoId"
                                            class="btn ftx-btn-action btn-azul btn-submit-spin">
                                            <i class="fa-duotone fa-floppy-disk icon-space icon-pulse"></i> Atualizar
                                            Contrato
                                        </button>
                                    }
                                    else
                                    {
                                        <button id="btnAdiciona" type="button" value="Submit" asp-page-handler="Submit"
                                            class="btn ftx-btn-action btn-azul btn-submit-spin">
                                            <i class="fa-duotone fa-floppy-disk icon-space icon-pulse"></i> Criar Contrato
                                        </button>
                                    }
                                    <a asp-page="./Index" class="btn btn-ftx-fechar" data-ftx-loading>
                                        <i class="fa-duotone fa-circle-xmark icon-space icon-pulse"></i> Cancelar
                                        Operação
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</form>

<script>
    function calculate(args) {
        try {
            if (args === undefined) return;

            try {
                var valorunitario = args.data.valorunitario;
                var quantidade = args.data.quantidade;

                valorunitario = brToFloat(valorunitario);
                quantidade = brToFloat(quantidade);

                var valortotal = valorunitario * quantidade;

                if (args.column.headerText == "Valor Total") {
                    $(args.cell).text(floatToBR(valortotal));
                }

                var gridObj = document.getElementById('grdVeiculos').ej2_instances[0];
                var rows = gridObj.getRows();
                var totalGrid = 0;

                for (var i = 0; i < rows.length; i++) {
                    var qtdCell = gridObj.getRows()[i].cells[2];
                    var valCell = gridObj.getRows()[i].cells[3];
                    if (!qtdCell || !valCell) continue;

                    var qtd = brToFloat(qtdCell.innerText);
                    var val = brToFloat(valCell.innerText);
                    totalGrid += (val * qtd);
                }

                $("#txtTotal").val(floatToBR(totalGrid));
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "calculate.inner", error);
            }
        } catch (error) {
            Alerta.TratamentoErroComLinha("Upsert.cshtml", "calculate", error);
        }
    }
</script>

@section ScriptsBlock {

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
        crossorigin="anonymous" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        crossorigin="anonymous"></script>

    <script>
        Number.prototype.format = function (n, x, s, c) {
            try {
                var re = '\\d(?=(\\d{' + (x || 3) + '})+' + (n > 0 ? '\\D' : '$') + ')',
                    num = this.toFixed(Math.max(0, ~~n));
                return (c ? num.replace('.', c) : num).replace(new RegExp(re, 'g'), '$&' + (s || ','));
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "format", error);
            }
        };

        function brToFloat(v) {
            try {
                if (v == null) return 0;
                if (typeof v === 'number') return v;
                v = (v + '').replace(/\s/g, '').replace(/\./g, '').replace(',', '.').replace('R$', '').replace('R$&nbsp;', '');
                var n = parseFloat(v);
                return isNaN(n) ? 0 : n;
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "brToFloat", error);
                return 0;
            }
        }

        function floatToBR(v) {
            try {
                return (isNaN(v) ? 0 : v).toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "floatToBR", error);
                return "0,00";
            }
        }

        $(document).ready(function () {
            try {
                const CONTRATO_ID = '@Model.ContratoObj.Contrato.ContratoId';
                const isEdit = (CONTRATO_ID && CONTRATO_ID !== '00000000-0000-0000-0000-000000000000');

                document.getElementById("divListaRepactuacoes").style.display = "none";
                document.getElementById("divVeiculosAdd").style.display = "none";
                document.getElementById("divVeiculosEdit").style.display = "none";
                document.getElementById("divTerceirizacao").style.display = "none";

                if (isEdit) {
                    $("#lblValor").text("Valor Repactuado (R$)");
                    document.getElementById("lblValor").textContent = "Valor Repactuado (R$)";

                    (function formatValorInicial() {
                        try {
                            var raw = '@Model.ContratoObj.Contrato.Valor';
                            var num = brToFloat(raw);
                            document.getElementById("valor").value = floatToBR(num);
                        } catch (error) {
                            Alerta.TratamentoErroComLinha("Upsert.cshtml", "formatValorInicial", error);
                        }
                    })();

                    $("#lstTipoContrato").attr('disabled', 'disabled');

                    document.getElementById("divListaRepactuacoes").style.display = "block";

                    $.ajax({
                        url: "api/Contrato/RepactuacaoList",
                        method: "GET",
                        data: { id: CONTRATO_ID },
                        success: function (res) {
                            try {
                                var option = '<option>-- Selecione uma Repactuação --</option>';
                                if (res != null && res.data && res.data.length) {
                                    res.data.forEach(function (obj) {
                                        option += '<option value="' + obj.repactuacaoContratoId + '">';
                                        option += obj.repactuacao;
                                        option += '</option>';
                                    });
                                }
                                $('#lstRepactuacao').empty().append(option);
                            } catch (error) {
                                Alerta.TratamentoErroComLinha("Upsert.cshtml", "RepactuacaoList.success", error);
                            }
                        },
                        error: function (err) {
                            Alerta.TratamentoErroComLinha("Upsert.cshtml", "RepactuacaoList.error", err);
                        }
                    });

                    var tipo = '@Model.ContratoObj.Contrato.TipoContrato';
                    if (tipo === 'Locação' || tipo === 'Loca&#xE7;&#xE3;o') {
                        document.getElementById("divVeiculosEdit").style.display = "block";
                        var gridObj = document.getElementById('grdVeiculos').ej2_instances[0];
                        gridObj.element.classList.add('disable-element');
                    }
                    if (tipo === 'Terceirização' || tipo === 'Terceiriza&#xE7;&#xE3;o') {
                    }
                } else {
                    document.getElementById("chkAtivo").checked = true;
                }

                $('#lstTipoContrato').on('change', function () {
                    try {
                        if (!isEdit) {
                            document.getElementById("divListaRepactuacoes").style.display = "none";
                        }

                        document.getElementById("divVeiculosAdd").style.display = "none";
                        document.getElementById("divVeiculosEdit").style.display = "none";
                        document.getElementById("divTerceirizacao").style.display = "none";

                        if (this.value === 'Terceirização') {
                            document.getElementById("divTerceirizacao").style.display = "block";
                            document.getElementById("chkoperadores").checked = false;
                            document.getElementById("chkmotoristas").checked = false;
                            document.getElementById("chklavadores").checked = false;
                            document.getElementById("chkencarregados").checked = false;
                            document.getElementById("chkmotoristas").disabled = false;
                        }

                        if (this.value === 'Locação') {
                            if (isEdit) {
                                document.getElementById("divVeiculosEdit").style.display = "block";
                            } else {
                                document.getElementById("divVeiculosAdd").style.display = "block";
                            }
                        }
                    } catch (error) {
                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "lstTipoContrato.change", error);
                    }
                });

                $('#chkencarregados').on('change', function () {
                    try {
                        if (!document.getElementById("chkencarregados").checked) {
                            $("#CustoMensalEncarregados").attr("disabled", "true").val("");
                            $("#txtQtdEncarregados").attr("disabled", "true").val("");
                        } else {
                            $("#CustoMensalEncarregados").removeAttr("disabled");
                            $("#txtQtdEncarregados").removeAttr("disabled");
                        }
                    } catch (error) {
                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "chkencarregados.change", error);
                    }
                });

                $('#chkoperadores').on('change', function () {
                    try {
                        if (!document.getElementById("chkoperadores").checked) {
                            $("#CustoMensalOperadores").attr("disabled", "true").val("");
                            $("#txtQtdOperadores").attr("disabled", "true").val("");
                        } else {
                            $("#CustoMensalOperadores").removeAttr("disabled");
                            $("#txtQtdOperadores").removeAttr("disabled");
                        }
                    } catch (error) {
                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "chkoperadores.change", error);
                    }
                });

                $('#chkmotoristas').on('change', function () {
                    try {
                        if (!document.getElementById("chkmotoristas").checked) {
                            $("#CustoMensalMotoristas").attr("disabled", "true").val("");
                            $("#txtQtdMotoristas").attr("disabled", "true").val("");
                        } else {
                            $("#CustoMensalMotoristas").removeAttr("disabled");
                            $("#txtQtdMotoristas").removeAttr("disabled");
                        }
                    } catch (error) {
                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "chkmotoristas.change", error);
                    }
                });

                $('#chklavadores').on('change', function () {
                    try {
                        if (!document.getElementById("chklavadores").checked) {
                            $("#CustoMensalLavadores").attr("disabled", "true").val("");
                            $("#txtQtdLavadores").attr("disabled", "true").val("");
                        } else {
                            $("#CustoMensalLavadores").removeAttr("disabled");
                            $("#txtQtdLavadores").removeAttr("disabled");
                        }
                    } catch (error) {
                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "chklavadores.change", error);
                    }
                });

                // Transforma Objeto do Contrato em Camel Case enquanto digita
                $('#lstObjeto').on('input', function () {
                    try {
                        var cursorPos = this.selectionStart;
                        var texto = $(this).val();
                        var textoCamelCase = texto.toLowerCase().replace(/(?:^|\s)\S/g, function (letra) {
                            return letra.toUpperCase();
                        });
                        $(this).val(textoCamelCase);
                        this.setSelectionRange(cursorPos, cursorPos);
                    } catch (error) {
                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "lstObjeto.input", error);
                    }
                });

            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "document.ready", error);
            }
        });

        $("#btnAdiciona").click(function (event) {
            try {
                event.preventDefault();
                InsereRegistro();
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "btnAdiciona.click", error);
            }
        });

        $("#btnEdita").click(function (event) {
            try {
                event.preventDefault();
                InsereRegistro();
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "btnEdita.click", error);
            }
        });

        function InsereRegistro() {
            try {
                if (document.getElementById("txtAno").value === "") {
                    Alerta.Erro("Informação Ausente", "O ano do contrato é obrigatório");
                    return;
                }

                if (document.getElementById("txtNumeroContrato").value === "") {
                    Alerta.Erro("Informação Ausente", "O número do contrato é obrigatório");
                    return;
                }

                if (document.getElementById("txtNumeroProcesso").value === "") {
                    Alerta.Erro("Informação Ausente", "O número do processo é obrigatório");
                    return;
                }

                if (document.getElementById("txtAnoProcesso").value === "") {
                    Alerta.Erro("Informação Ausente", "O ano do processo é obrigatório");
                    return;
                }

                if (document.getElementById("lstVigencia").value === "") {
                    Alerta.Erro("Informação Ausente", "A vigência do contrato é obrigatória");
                    return;
                }

                if (document.getElementById("lstProrrogacao").value === "") {
                    Alerta.Erro("Informação Ausente", "A prorrogação do contrato é obrigatória");
                    return;
                }

                if (document.getElementById("ContratoObj_Contrato_FornecedorId").value === "") {
                    Alerta.Erro("Informação Ausente", "O fornecedor do contrato é obrigatório");
                    return;
                }

                if (document.getElementById("lstObjeto").value === "") {
                    Alerta.Erro("Informação Ausente", "O objeto do contrato é obrigatório");
                    return;
                }

                if (document.getElementById("lstTipoContrato").value === "") {
                    Alerta.Erro("Informação Ausente", "O tipo do contrato é obrigatório");
                    return;
                }

                if (document.getElementById("txtDataInicio").value === "") {
                    Alerta.Erro("Informação Ausente", "A data de início do contrato é obrigatória");
                    return;
                }

                if (document.getElementById("txtDataFim").value === "") {
                    Alerta.Erro("Informação Ausente", "A data de fim do contrato é obrigatória");
                    return;
                }

                if (document.getElementById("valor").value === "") {
                    Alerta.Erro("Informação Ausente", "O valor do contrato é obrigatório");
                    return;
                }

                if (document.getElementById("lstTipoContrato").value === "Terceirização" && document.getElementById("chkencarregados").checked === false) {
                    Alerta.Erro("Informação Ausente", "Para um contrato de terceirização, você deve informar ao menos 01 encarregado.");
                    return;
                }

                if (document.getElementById("lstTipoContrato").value === "Terceirização"
                    && document.getElementById("chkoperadores").checked === false
                    && document.getElementById("chkmotoristas").checked === false
                    && document.getElementById("chklavadores").checked === false) {
                    Alerta.Erro("Informação Ausente", "Para um contrato de terceirização, você deve escolher ao menos uma opção dentre Operadores/Motoristas/Lavadores.");
                    return;
                }

                if (document.getElementById("lstTipoContrato").value === "Terceirização"
                    && document.getElementById("chkencarregados").checked === true
                    && (document.getElementById("CustoMensalEncarregados").value === '' || document.getElementById("txtQtdEncarregados").value === '')) {
                    Alerta.Erro("Informação Ausente", "Você deve informar a quantidade e o custo unitário mensal dos Encarregados.");
                    return;
                }

                if (document.getElementById("lstTipoContrato").value === "Terceirização"
                    && document.getElementById("chkoperadores").checked === true
                    && (document.getElementById("CustoMensalOperadores").value === '' || document.getElementById("txtQtdOperadores").value === '')) {
                    Alerta.Erro("Informação Ausente", "Você deve informar a quantidade e o custo unitário mensal dos Operadores.");
                    return;
                }

                if (document.getElementById("lstTipoContrato").value === "Terceirização"
                    && document.getElementById("chkmotoristas").checked === true
                    && (document.getElementById("CustoMensalMotoristas").value === '' || document.getElementById("txtQtdMotoristas").value === '')) {
                    Alerta.Erro("Informação Ausente", "Você deve informar a quantidade e o custo unitário mensal dos Motoristas.");
                    return;
                }

                if (document.getElementById("lstTipoContrato").value === "Terceirização"
                    && document.getElementById("chklavadores").checked === true
                    && (document.getElementById("CustoMensalLavadores").value === '' || document.getElementById("txtQtdLavadores").value === '')) {
                    Alerta.Erro("Informação Ausente", "Você deve informar a quantidade e o custo unitário mensal dos Lavadores.");
                    return;
                }

                var gridObj = document.getElementById('grdVeiculos').ej2_instances[0];
                var getRows = gridObj.getRows();

                if (document.getElementById("lstTipoContrato").value === "Locação" && getRows.length === 0 &&
                    ('@Model.ContratoObj.Contrato.ContratoId' === null || '@Model.ContratoObj.Contrato.ContratoId' === '00000000-0000-0000-0000-000000000000')) {
                    Alerta.Erro("Informação Ausente", "Para um contrato de locação, você deve informar ao menos um veículo.");
                    return;
                }

                if ('@Model.ContratoObj.Contrato.ContratoId' === null || '@Model.ContratoObj.Contrato.ContratoId' === '00000000-0000-0000-0000-000000000000') {

                    var valorcontrato = brToFloat($('#valor').val());

                    var valorencarregado = 0, valoroperador = 0, valormotorista = 0, valorlavador = 0;

                    if (document.getElementById("lstTipoContrato").value === "Terceirização") {
                        valorencarregado = brToFloat($('#CustoMensalEncarregados').val());
                        valoroperador = brToFloat($('#CustoMensalOperadores').val());
                        valormotorista = brToFloat($('#CustoMensalMotoristas').val());
                        valorlavador = brToFloat($('#CustoMensalLavadores').val());
                    }

                    var objContrato = JSON.stringify({
                        "AnoContrato": $('#txtAno').val(),
                        "NumeroContrato": $('#txtNumeroContrato').val(),
                        "NumeroProcesso": $('#txtNumeroProcesso').val(),
                        "AnoProcesso": $('#txtAnoProcesso').val(),
                        "Vigencia": $('#lstVigencia').val(),
                        "Prorrogacao": $('#lstProrrogacao').val(),
                        "FornecedorId": $('#ContratoObj_Contrato_FornecedorId').val(),
                        "Objeto": $('#lstObjeto').val(),
                        "DataInicio": $('#txtDataInicio').val(),
                        "DataFim": $('#txtDataFim').val(),
                        "TipoContrato": $('#lstTipoContrato').val(),
                        "Valor": valorcontrato,
                        "ContratoEncarregados": $("#chkencarregados").is(':checked'),
                        "CustoMensalEncarregado": valorencarregado,
                        "QuantidadeEncarregado": $('#txtQtdEncarregados').val(),
                        "ContratoOperadores": $("#chkoperadores").is(':checked'),
                        "CustoMensalOperador": valoroperador,
                        "QuantidadeOperador": $('#txtQtdOperadores').val(),
                        "ContratoMotoristas": $("#chkmotoristas").is(':checked'),
                        "CustoMensalMotorista": valormotorista,
                        "QuantidadeMotorista": $('#txtQtdMotoristas').val(),
                        "ContratoLavadores": $("#chklavadores").is(':checked'),
                        "CustoMensalLavador": valorlavador,
                        "QuantidadeLavador": $('#txtQtdLavadores').val(),
                        "Status": document.getElementById("chkAtivo").checked
                    });

                    $.ajax({
                        type: "post",
                        url: "api/Contrato/InsereContrato",
                        contentType: "application/json; charset=utf-8",
                        dataType: "json",
                        data: objContrato,
                        success: function (data) {
                            try {
                                var RepactuacaoId = data.data;

                                if (document.getElementById("lstTipoContrato").value === "Locação") {
                                    var gridObj = document.getElementById('grdVeiculos').ej2_instances[0];
                                    var getRows = gridObj.getRows();

                                    for (var i = 0; i < getRows.length; i++) {
                                        var valorunitario = brToFloat(gridObj.getRows()[i].cells[3].innerHTML);

                                        var objItemContrato = JSON.stringify({
                                            "NumItem": gridObj.getRows()[i].cells[0].innerHTML,
                                            "Descricao": gridObj.getRows()[i].cells[1].innerHTML,
                                            "Quantidade": gridObj.getRows()[i].cells[2].innerHTML,
                                            "ValorUnitario": valorunitario,
                                            "RepactuacaoContratoId": RepactuacaoId
                                        });

                                        $.ajax({
                                            type: "post",
                                            url: "api/Contrato/InsereItemContrato",
                                            contentType: "application/json; charset=utf-8",
                                            dataType: "json",
                                            data: objItemContrato
                                        });
                                    }
                                }

                                if (document.getElementById("lstTipoContrato").value === "Terceirização") {
                                    var objRepactuacaoTerceirizacao = JSON.stringify({
                                        "RepactuacaoContratoId": RepactuacaoId,
                                        "DataRepactuacao": $('#txtDataInicio').val(),
                                        "ValorEncarregado": valorencarregado,
                                        "QtdEncarregados": document.getElementById("txtQtdEncarregados").value,
                                        "ValorOperador": valoroperador,
                                        "QtdOPeradores": document.getElementById("txtQtdOperadores").value,
                                        "ValorMotorista": valormotorista,
                                        "QtdMotoristas": document.getElementById("txtQtdMotoristas").value,
                                        "ValorLavador": valorlavador,
                                        "QtdLavadores": document.getElementById("txtQtdLavadores").value
                                    });

                                    $.ajax({
                                        type: "post",
                                        url: "api/Contrato/InsereRepactuacaoTerceirizacao",
                                        contentType: "application/json; charset=utf-8",
                                        dataType: "json",
                                        data: objRepactuacaoTerceirizacao
                                    });
                                }

                                if (document.getElementById("lstTipoContrato").value === "Serviços") {
                                    var objRepactuacaoServicos = JSON.stringify({
                                        "RepactuacaoContratoId": RepactuacaoId,
                                        "DataRepactuacao": $('#txtDataInicio').val(),
                                        "Valor": valorcontrato
                                    });

                                    $.ajax({
                                        type: "post",
                                        url: "api/Contrato/InsereRepactuacaoServicos",
                                        contentType: "application/json; charset=utf-8",
                                        dataType: "json",
                                        data: objRepactuacaoServicos
                                    });
                                }

                                location.replace("/contrato");
                                AppToast.show('Verde', "Contrato Adicionado com Sucesso!", 2000);
                            } catch (error) {
                                Alerta.TratamentoErroComLinha("Upsert.cshtml", "InsereContrato.success", error);
                            }
                        },
                        error: function (data) {
                            try {
                                AppToast.show('Vermelho', data.message, 2000);
                                console.log(data);
                            } catch (error) {
                                Alerta.TratamentoErroComLinha("Upsert.cshtml", "InsereContrato.error", error);
                            }
                        }
                    });
                } else {
                    var valorcontrato = brToFloat($('#valor').val());

                    var valorencarregado = 0, valoroperador = 0, valormotorista = 0, valorlavador = 0;

                    if (document.getElementById("lstTipoContrato").value === "Terceirização") {
                        valorencarregado = brToFloat($('#CustoMensalEncarregados').val());
                        valoroperador = brToFloat($('#CustoMensalOperadores').val());
                        valormotorista = brToFloat($('#CustoMensalMotoristas').val());
                        valorlavador = brToFloat($('#CustoMensalLavadores').val());
                    }

                    var objContrato = JSON.stringify({
                        "ContratoId": '@Model.ContratoObj.Contrato.ContratoId',
                        "AnoContrato": $('#txtAno').val(),
                        "NumeroContrato": $('#txtNumeroContrato').val(),
                        "NumeroProcesso": $('#txtNumeroProcesso').val(),
                        "AnoProcesso": $('#txtAnoProcesso').val(),
                        "Vigencia": $('#lstVigencia').val(),
                        "Prorrogacao": $('#lstProrrogacao').val(),
                        "FornecedorId": $('#ContratoObj_Contrato_FornecedorId').val(),
                        "Objeto": $('#lstObjeto').val(),
                        "DataInicio": $('#txtDataInicio').val(),
                        "DataFim": $('#txtDataFim').val(),
                        "TipoContrato": $('#lstTipoContrato').val(),
                        "Valor": valorcontrato,
                        "ContratoEncarregados": $("#chkencarregados").is(':checked'),
                        "CustoMensalEncarregado": valorencarregado,
                        "QuantidadeEncarregado": $('#txtQtdEncarregados').val(),
                        "ContratoOperadores": $("#chkoperadores").is(':checked'),
                        "CustoMensalOperador": valoroperador,
                        "QuantidadeOperador": $('#txtQtdOperadores').val(),
                        "ContratoMotoristas": $("#chkmotoristas").is(':checked'),
                        "CustoMensalMotorista": valormotorista,
                        "QuantidadeMotorista": $('#txtQtdMotoristas').val(),
                        "ContratoLavadores": $("#chklavadores").is(':checked'),
                        "CustoMensalLavador": valorlavador,
                        "QuantidadeLavador": $('#txtQtdLavadores').val(),
                        "Status": document.getElementById("chkAtivo").checked
                    });

                    $.ajax({
                        type: "post",
                        url: "api/Contrato/EditaContrato",
                        contentType: "application/json; charset=utf-8",
                        dataType: "json",
                        data: objContrato,
                        success: function () {
                            try {
                                location.replace("/contrato");
                                AppToast.show('Verde', "Contrato Atualizado com Sucesso!");
                            } catch (error) {
                                Alerta.TratamentoErroComLinha("Upsert.cshtml", "EditaContrato.success", error);
                            }
                        },
                        error: function (data) {
                            try {
                                AppToast.show('Vermelho', data.message);
                                console.log(data);
                            } catch (error) {
                                Alerta.TratamentoErroComLinha("Upsert.cshtml", "EditaContrato.error", error);
                            }
                        }
                    });
                }

            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "InsereRegistro", error);
            }
        }
    </script>

    <script>
        function moeda(a, e, r, t) {
            try {
                let n = "", h = j = 0, u = tamanho2 = 0, l = ajd2 = "",
                    o = window.Event ? t.which : t.keyCode;
                if (13 == o || 8 == o) return !0;
                if (n = String.fromCharCode(o), -1 == "0123456789".indexOf(n)) return !1;
                for (u = a.value.length, h = 0; h < u && ("0" == a.value.charAt(h) || a.value.charAt(h) == r); h++);
                for (l = ""; h < u; h++) -1 != "0123456789".indexOf(a.value.charAt(h)) && (l += a.value.charAt(h));
                if (l += n, 0 == (u = l.length) && (a.value = ""), 1 == u && (a.value = "0" + r + "0" + l), 2 == u && (a.value = "0" + r + l), u > 2) {
                    for (ajd2 = "", j = 0, h = u - 3; h >= 0; h--)
                        3 == j && (ajd2 += e, j = 0), ajd2 += l.charAt(h), j++;
                    for (a.value = "", tamanho2 = ajd2.length, h = tamanho2 - 1; h >= 0; h--) a.value += ajd2.charAt(h);
                    a.value += r + l.substr(u - 2, u)
                }
                return !1
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "moeda", error);
            }
        }
    </script>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            try {
                function actionComplete(args) {
                    try {
                        if (args.requestType == "save") {
                            var gridObj = document.getElementById('grdVeiculos').ej2_instances[0];
                            gridObj.dataSource.shift();
                            gridObj.dataSource.push(args.data);
                            gridObj.refresh();
                        }
                    } catch (error) {
                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "actionComplete", error);
                    }
                }

                document.getElementById("lstRepactuacao").addEventListener("change", function () {
                    try {
                        let RepactuacaoContratoId = document.getElementById("lstRepactuacao").value;

                        if (document.getElementById("lstTipoContrato").value === "Locação") {

                            if (RepactuacaoContratoId === "-- Selecione uma Repactuação --" || RepactuacaoContratoId === "") {
                                document.getElementById("divVeiculosEdit").style.display = "none";
                                return;
                            } else {
                                document.getElementById("divVeiculosEdit").style.display = "block";
                            }

                            var grid = document.getElementById('grdVeiculos2').ej2_instances[0];

                            $.ajax({
                                url: 'api/GridContrato/DataSource',
                                type: "GET",
                                success: function (res) {
                                    try {
                                        var objItens = res || [];
                                        objItens = objItens.filter(function (obj) {
                                            return obj.repactuacaoId === RepactuacaoContratoId;
                                        });

                                        grid.dataSource = objItens;

                                        var valortotal = 0;
                                        for (var i = 0; i < objItens.length; i++) {
                                            valortotal += (objItens[i].valortotal || 0);
                                        }
                                        document.getElementById("txtTotalEdit").value = floatToBR(valortotal);
                                    } catch (error) {
                                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "DataSource.success", error);
                                    }
                                },
                                error: function (err) {
                                    Alerta.TratamentoErroComLinha("Upsert.cshtml", "DataSource.error", err);
                                }
                            });
                        }

                        if (document.getElementById("lstTipoContrato").value === "Terceirização") {

                            document.getElementById("divTerceirizacao").style.display = "block";

                            if (RepactuacaoContratoId === "-- Selecione uma Repactuação --" || RepactuacaoContratoId === "") {
                                document.getElementById("divTerceirizacao").style.display = "none";
                                return;
                            }

                            $.ajax({
                                type: "GET",
                                url: 'api/Contrato/RecuperaRepactuacaoTerceirizacao',
                                data: { RepactuacaoContratoId: RepactuacaoContratoId },
                                contentType: "application/json",
                                dataType: "json",
                                success: function (objRep) {
                                    try {
                                        var o = objRep && objRep.objRepactuacaoTerceirizacao ? objRep.objRepactuacaoTerceirizacao : null;
                                        if (!o) return;

                                        if (o.valorEncarregado != null && o.valorEncarregado != 0) {
                                            document.getElementById("chkencarregados").checked = true;
                                            document.getElementById("CustoMensalEncarregados").value = floatToBR(o.valorEncarregado);
                                            document.getElementById("txtQtdEncarregados").value = o.qtdEncarregados || "";
                                        } else {
                                            document.getElementById("chkencarregados").checked = false;
                                            document.getElementById("CustoMensalEncarregados").value = "";
                                            document.getElementById("txtQtdEncarregados").value = "";
                                        }

                                        if (o.valorOperador != null && o.valorOperador != 0) {
                                            document.getElementById("chkoperadores").checked = true;
                                            document.getElementById("CustoMensalOperadores").value = floatToBR(o.valorOperador);
                                            document.getElementById("txtQtdOperadores").value = o.qtdOperadores || "";
                                        } else {
                                            document.getElementById("chkoperadores").checked = false;
                                            document.getElementById("CustoMensalOperadores").value = "";
                                            document.getElementById("txtQtdOperadores").value = "";
                                        }

                                        if (o.valorMotorista != null && o.valorMotorista != 0) {
                                            document.getElementById("chkmotoristas").checked = true;
                                            document.getElementById("CustoMensalMotoristas").value = floatToBR(o.valorMotorista);
                                            document.getElementById("txtQtdMotoristas").value = o.qtdMotoristas || "";
                                        } else {
                                            document.getElementById("chkmotoristas").checked = false;
                                            document.getElementById("CustoMensalMotoristas").value = "";
                                            document.getElementById("txtQtdMotoristas").value = "";
                                        }

                                        if (o.valorLavador != null && o.valorLavador != 0) {
                                            document.getElementById("chklavadores").checked = true;
                                            document.getElementById("CustoMensalLavadores").value = floatToBR(o.valorLavador);
                                            document.getElementById("txtQtdLavadores").value = o.qtdLavadores || "";
                                        } else {
                                            document.getElementById("chklavadores").checked = false;
                                            document.getElementById("CustoMensalLavadores").value = "";
                                            document.getElementById("txtQtdLavadores").value = "";
                                        }

                                    } catch (error) {
                                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "RecuperaTerceirizacao.success", error);
                                    }
                                },
                                error: function (data) {
                                    Alerta.TratamentoErroComLinha("Upsert.cshtml", "RecuperaTerceirizacao.error", data);
                                }
                            });

                        }

                        if (document.getElementById("lstTipoContrato").value === "Serviços") {

                            if (RepactuacaoContratoId === "-- Selecione uma Repactuação --" || RepactuacaoContratoId === "") {
                                document.getElementById("valor").value = floatToBR(0);
                                return;
                            }

                            $.ajax({
                                type: "GET",
                                url: 'api/Contrato/RecuperaRepactuacaoServicos',
                                data: { RepactuacaoContratoId: RepactuacaoContratoId },
                                contentType: "application/json",
                                dataType: "json",
                                success: function (data) {
                                    try {
                                        var v = (data && data.data && data.data.valor) ? data.data.valor : 0;
                                        document.getElementById("valor").value = floatToBR(brToFloat(v));
                                    } catch (error) {
                                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "RecuperaServicos.success", error);
                                    }
                                },
                                error: function (data) {
                                    Alerta.TratamentoErroComLinha("Upsert.cshtml", "RecuperaServicos.error", data);
                                }
                            });
                        }

                    } catch (error) {
                        Alerta.TratamentoErroComLinha("Upsert.cshtml", "lstRepactuacao.change", error);
                    }
                });
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "DOMContentLoaded", error);
            }
        });
    </script>

    <script>
        var btnAtualiza = document.getElementById("btnAtualizaTotal");

        if (btnAtualiza) {
            btnAtualiza.addEventListener("click", function (event) {
                try {
                    event.preventDefault();
                    AtualizaTotal();
                } catch (error) {
                    Alerta.TratamentoErroComLinha("Upsert.cshtml", "btnAtualizaTotal.click", error);
                }
            });
        }

        function AtualizaTotal() {
            try {
                var gridObj = document.getElementById('grdVeiculos').ej2_instances[0];
                var getRows = gridObj.getRows();
                var valortotalgrid = 0;

                for (var i = 0; i < getRows.length; i++) {
                    var qtd = brToFloat(gridObj.getRows()[i].cells[2].innerHTML);
                    var val = brToFloat(gridObj.getRows()[i].cells[3].innerHTML);
                    valortotalgrid += (val * qtd);
                }

                var valortotalgeral = valortotalgrid;
                $("#txtTotal").val(floatToBR(valortotalgeral));
            } catch (error) {
                Alerta.TratamentoErroComLinha("Upsert.cshtml", "AtualizaTotal", error);
            }
        }
    </script>
}