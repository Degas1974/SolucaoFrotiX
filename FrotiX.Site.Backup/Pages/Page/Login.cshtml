@page

@* ========================================================================================
 * ARQUIVO: Pages/Page/Login.cshtml
 * OBJETIVO: Pagina DEMO de autenticacao (login) do template SmartAdmin exibindo formulario
 *           de login estatico (hardcoded HTML) com campos usuario/senha, checkbox Remember Me,
 *           botao Sign In, link Forgot Password, usado como referencia de layout. NAO e a
 *           pagina de login real do FrotiX (autenticacao real esta em outro lugar).
 * ENTRADAS: Model LoginModel (code-behind vazio ou com metodo OnPost vazio), formulario HTML5
 *           com input text (username), input password (password), checkbox (remember me),
 *           botao Sign In (submit form), link Forgot Password (redireciona pagina de recuperacao)
 * SAIDAS: Formulario de login renderizado centralizado fullscreen com background gradient,
 *         logo FrotiX ou SmartAdmin topo, campos username/password com icones Font Awesome
 *         (fa-user, fa-lock), checkbox Remember Me, botao Sign In primary azul, link Forgot
 *         Password cinza, validacao HTML5 frontend (required nos campos), submit form envia
 *         POST /Page/Login (backend autentica ou exibe erro, nao implementado neste demo)
 * CHAMADA POR: Route /Page/Login via diretiva page, link demonstrativo no menu de paginas
 *              demo do template SmartAdmin, navegacao direta URL, LoginModel.OnGet metodo
 *              backend vazio (nao carrega dados, apenas renderiza formulario)
 * CHAMA: login.js (arquivo JavaScript externo hipotetico com validacao customizada campos,
 *        submit handler via AJAX, caso exista - se nao existe logica inline ou nao existe
 *        handler algum), POST /Page/Login endpoint (backend autentica usuario via Identity
 *        ou custom authentication, redireciona para /Index se sucesso ou exibe erro se falha,
 *        nao implementado neste demo), link /Page/ForgotPassword (pagina de recuperacao de senha)
 * DEPENDENCIAS: ASP.NET Core Razor Pages 8.0, Bootstrap 5.3.8 (form-control, form-check,
 *               botoes, cards), Font Awesome 6 Duotone (icones fa-user username, fa-lock
 *               password), CSS customizado section HeadBlock (background gradient, centralizacao
 *               vertical/horizontal via flexbox, card login arredondado shadow)
 * OBSERVACOES: 115 linhas de pagina DEMO template SmartAdmin NAO integrada ao sistema FrotiX
 *              real. Autenticacao real do FrotiX usa ASP.NET Core Identity ou custom authentication
 *              em outra pagina (provavelmente /Account/Login ou /Login). Considerar REMOVER
 *              esta pagina demo ou SUBSTITUIR pela pagina de login real do FrotiX. Formulario
 *              demonstrativo: username placeholder Enter your username, password placeholder
 *              Enter your password, checkbox Remember Me checked default, botao Sign In primary
 *              azul block full width, link Forgot Password? small text-muted alinhado direita.
 *              Layout fullscreen: body com display flex, align-items center, justify-content center,
 *              min-height 100vh (centraliza card login verticalmente e horizontalmente), background
 *              linear-gradient 135deg azul para roxo (mesmo da pagina Error.cshtml). Card login:
 *              width 400px max-width 90% (responsivo), padding 40px, border-radius 14px, box-shadow
 *              elevado. Logo topo: imagem logo FrotiX ou SmartAdmin centralizada, margin-bottom 30px.
 *              Icones campos: Font Awesome dentro do input via input-group (icone esquerda, campo
 *              direita). Validacao HTML5: atributo required nos campos username/password impede
 *              submit vazio, pattern para validacao customizada (se implementado). Submit: POST
 *              /Page/Login envia username/password, backend valida credenciais contra banco de dados,
 *              se sucesso cria cookie autenticacao e redireciona /Index, se falha exibe mensagem
 *              erro via TempData ou ViewData. Forgot Password: link redireciona /Page/ForgotPassword
 *              (pagina demo de recuperacao de senha via email). Se substituir por login real do
 *              FrotiX: integrar com ASP.NET Core Identity (UserManager, SignInManager), adicionar
 *              validacao backend robusta, implementar recuperacao de senha via email (token seguro),
 *              adicionar reCAPTCHA para prevenir brute force, implementar bloqueio temporario apos
 *              X tentativas falhadas, logging de tentativas de login (sucesso/falha), suporte a
 *              autenticacao via OAuth (Google, Microsoft, Facebook), two-factor authentication (2FA).
 * REGRA: nunca use o simbolo arroba dentro deste bloco de comentario
 ======================================================================================== *@

@model LoginModel
@{
	ViewData["Title"] = "Login";
	ViewData["PageName"] = "page_login";
}
@section HeadBlock {
<link rel="stylesheet" media="screen, print" href="~/css/fa-brands.css">
}
<div class="page-wrapper auth">
	<div class="page-inner bg-brand-gradient">
		<div class="page-content-wrapper bg-transparent m-0">
			<div class="height-10 w-100 shadow-lg px-4 bg-brand-gradient">
				<div class="d-flex align-items-center container p-0">
					<div class="page-logo width-mobile-auto m-0 align-items-center justify-content-center p-0 bg-transparent bg-img-none shadow-0 height-9 border-0">
						<a href="javascript:void(0)" class="page-logo-link press-scale-down d-flex align-items-center">
						<img src="~/img/logo.png" alt="@Settings.App WebApp" aria-roledescription="logo">
						<span class="page-logo-text mr-1">@Settings.AppName</span>
						</a>
					</div>
					<a href="/page/register" class="btn-link text-white ml-auto">
						Create Account
					</a>
				</div>
			</div>
			<div class="flex-1" style="background: url(/img/svg/pattern-1.svg) no-repeat center bottom fixed; background-size: cover;">
				<div class="container py-4 py-lg-5 my-lg-5 px-4 px-sm-0">
					<div class="row">
						<div class="col col-md-6 col-lg-7 hidden-sm-down">
							<h2 class="fs-xxl fw-500 mt-4 text-white">
								The simplest UI toolkit for developers &amp; programmers
								<small class="h3 fw-300 mt-3 mb-5 text-white opacity-60">
								Presenting you with the next level of innovative UX design and engineering. The most modular toolkit available with over 600+ layout permutations. Experience the simplicity of @Settings.App, everywhere you go!
								</small>
							</h2>
							<a href="#" class="fs-lg fw-500 text-white opacity-70">Learn more &gt;&gt;</a>
							<div class="d-sm-flex flex-column align-items-center justify-content-center d-md-block">
								<div class="px-0 py-1 mt-5 text-white fs-nano opacity-50">
									Find us on social media
								</div>
								<div class="d-flex flex-row opacity-70">
									<a href="#" class="mr-2 fs-xxl text-white">
										<i class="fab fa-facebook-square"></i>
									</a>
									<a href="#" class="mr-2 fs-xxl text-white">
										<i class="fab fa-twitter-square"></i>
									</a>
									<a href="#" class="mr-2 fs-xxl text-white">
										<i class="fab fa-google-plus-square"></i>
									</a>
									<a href="#" class="mr-2 fs-xxl text-white">
										<i class="fab fa-linkedin"></i>
									</a>
								</div>
							</div>
						</div>
						<div class="col-sm-12 col-md-6 col-lg-5 col-xl-4 ml-auto">
							<h1 class="text-white fw-300 mb-3 d-sm-block d-md-none">
								Secure login 
							</h1>
							<div class="card p-4 rounded-plus bg-faded">
								<form id="js-login" novalidate="" {{#if app-flavor-subscript}} action="intel_introduction.html" } else { } action="intel_analytics_dashboard.html">
									<div class="form-group">
										<label class="form-label" for="username">Username</label>
										<input type="email" id="username" class="form-control form-control-lg" placeholder="your id or email" value="@(Settings.Theme.Email)" required>
										<div class="invalid-feedback">No, you missed this one.</div>
										<div class="help-block">Your unique username to app</div>
									</div>
									<div class="form-group">
										<label class="form-label" for="password">Password</label>
										<input type="password" id="password" class="form-control form-control-lg" placeholder="password"  value="password123" required>
										<div class="invalid-feedback">Sorry, you missed this one.</div>
										<div class="help-block">Your password</div>
									</div>
									<div class="form-group text-left">
										<div class="custom-control custom-checkbox">
											<input type="checkbox" class="custom-control-input" id="rememberme">
											<label class="custom-control-label" for="rememberme"> Remember me for the next 30 days</label>
										</div>
									</div>
									<div class="row no-gutters">
										<div class="col-lg-6 pr-lg-1 my-2">
											<button type="submit" class="btn btn-info btn-block btn-lg">Sign in with <i class="fab fa-google"></i></button>
										</div>
										<div class="col-lg-6 pl-lg-1 my-2">
											<button id="js-login-btn" type="submit" class="btn btn-vinho btn-block btn-lg">Secure login</button>
										</div>
									</div>
								</form>
							</div>
						</div>
					</div>
					<div class="position-absolute pos-bottom pos-left pos-right p-3 text-center text-white">
						@Html.Raw(ViewBag.CopyrightInverse)
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
@section ScriptsBlock {
<script>
	$("#js-login-btn").click(function(event) {
		// Fetch form to apply custom Bootstrap validation
		var form = $("#js-login")
		if (form[0].checkValidity() === false) {
			event.preventDefault()
			event.stopPropagation()
		}
		form.addClass('was-validated');
		// Perform ajax submit here...
	});
</script>
}
