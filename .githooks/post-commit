#!/usr/bin/env bash
set -euo pipefail

# Auto-push commits to main
current_branch="$(git rev-parse --abbrev-ref HEAD)"

if [[ "$current_branch" == "main" ]]; then
  git push origin main
else
  echo "[post-commit] Branch atual ($current_branch) != main. Push não executado." >&2
fi
